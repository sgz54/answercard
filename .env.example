# API mode: "mock" runs entirely in the browser with a local divination engine.
# Switch to "real" to talk to the FastAPI backend (see /backend).
VITE_API_MODE=mock
VITE_API_BASE_URL=http://localhost:8000
